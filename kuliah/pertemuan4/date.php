<?php 
	// echo date("l"); untuk hari
	// echo date("d"); untuk tanggal
	// echo date("M"); untuk bulan (nama bulan)
	// echo date("m"); untuk bulan (dengan angka)
	// echo date("Y"); untuk tahun
	// date untuk menampilkan tanggal dalam format tertentu
	// echo date("l, d-M-Y");

	// time
	// UNIX Timestamp / EPOCH time
	// detik yang sudah berlalu sejak 1 januari 1970
	// echo time();
	// echo date("l", time()+172800);
	// echo date("l", time()+60*60*24*200);
	// echo date("l",mktime(0, 0, 0, 4, 29, 2004)) ;
	
	// mktime
	// membuat sendiri setik
	// mktime(0,0,0,0,0,0,0)
	// mktime(jam, menit, detik, bulan, tangga, tahun)
	// echo mktime(0, 0, 0, 4, 29, 2004);

	// strtotime
	// echo date("l", strtotime("29 apr 2004"));


?>